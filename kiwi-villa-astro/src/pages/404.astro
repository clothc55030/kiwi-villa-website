---
import Layout from '../layouts/Layout.astro';

const pageTitle = '404 - 頁面不存在 | 澎湖期遇度假會館';
const pageDescription = '抱歉，您訪問的頁面不存在。請返回首頁或使用導航菜單繼續瀏覽。';
---

<Layout 
  title={pageTitle}
  description={pageDescription}
>
  <section class="min-h-[80vh] flex items-center justify-center bg-gradient-to-br from-primary/10 to-secondary/10">
    <div class="container max-w-2xl mx-auto px-4 text-center">
      <!-- 404 圖示 -->
      <div class="mb-8">
        <div class="text-[150px] font-bold text-primary/20 leading-none">404</div>
        <iconify-icon 
          icon="heroicons:face-frown" 
          class="w-24 h-24 text-primary/50 mx-auto -mt-16"
        ></iconify-icon>
      </div>
      
      <!-- 錯誤訊息 -->
      <h1 class="text-3xl md:text-4xl font-serif text-charcoal mb-4">
        找不到頁面
      </h1>
      <p class="text-lg text-text-secondary mb-8">
        哎呀！您要找的頁面似乎不存在，可能已經移動或刪除了。
      </p>
      
      <!-- 建議連結 -->
      <div class="space-y-4 mb-8">
        <p class="text-text-secondary">您可能在尋找以下頁面：</p>
        <div class="flex flex-wrap gap-4 justify-center">
          <a 
            href="/" 
            class="px-6 py-3 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors"
          >
            返回首頁
          </a>
          <a 
            href="/rooms" 
            class="px-6 py-3 bg-white text-primary border border-primary rounded-lg hover:bg-primary/10 transition-colors"
          >
            查看房型
          </a>
          <a 
            href="/location" 
            class="px-6 py-3 bg-white text-primary border border-primary rounded-lg hover:bg-primary/10 transition-colors"
          >
            位置資訊
          </a>
        </div>
      </div>
      
      <!-- 搜尋建議 -->
      <div class="mt-12 p-6 bg-white rounded-2xl shadow-light">
        <h2 class="text-xl font-semibold text-charcoal mb-4">需要幫助嗎？</h2>
        <p class="text-text-secondary mb-4">
          如果您在尋找特定內容，請嘗試使用導航菜單或直接聯繫我們。
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a 
            href="tel:+886933636373" 
            class="inline-flex items-center gap-2 text-primary hover:text-primary-dark transition-colors"
          >
            <iconify-icon icon="heroicons:phone" class="w-5 h-5"></iconify-icon>
            0933-636373
          </a>
          <span class="hidden sm:inline text-gray-300">|</span>
          <a 
            href="https://lin.ee/MpA4oCQ" 
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 text-primary hover:text-primary-dark transition-colors"
          >
            <iconify-icon icon="simple-icons:line" class="w-5 h-5"></iconify-icon>
            LINE 聯絡我們
          </a>
        </div>
      </div>
    </div>
  </section>
  
  <script>
    // 記錄 404 錯誤
    if (import.meta.env.PROD) {
      // 在生產環境中記錄 404 錯誤
      const errorData = {
        type: '404',
        url: window.location.href,
        referrer: document.referrer,
        timestamp: new Date().toISOString(),
      };
      
      // 可以發送到分析服務
      if (typeof gtag !== 'undefined') {
        gtag('event', 'exception', {
          description: '404_page_not_found',
          fatal: false,
          error_url: window.location.href,
        });
      }
    }
  </script>
</Layout>